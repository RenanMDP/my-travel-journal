import React from 'react';
import ReactDOM from "react-dom";
import './styles.scss';
import App from './App';
const renderDOM = document.querySelector("#root");

ReactDOM.render(
  <App />,
  renderDOM
);